[build-system]
requires = [
    "setuptools >= 40.9.0",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.isort]
line_length = 179

[tool.mypy]
ignore_missing_imports = true
files = "wikibaseintegrator/**/*.py,test/*.py"

[tool.pylint.format]
max-line-length = 179
